#Ternary Operator

# condition_if_true if condition else condition_if_false
is_friend = True
can_message = "can message" if is_friend else "not allowed"
print(can_message)

is_friend2 = False
can_message2 = "can message" if is_friend2 else "not allowed"
print(can_message2)